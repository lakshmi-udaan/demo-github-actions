name: Simple Usage

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: docker://pandoc/core:2.9
        with:
          args: "https://github.com/lakshmi-udaan/demo-github-actions/tree/main/pandoc/*.md > sample.md" # gets appended to pandoc command